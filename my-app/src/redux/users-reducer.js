let initialState = {
    usersData: [
        {id: 1, name: "User 1"},
        {id: 2, name: "User 2"},
        {id: 3, name: "User 3"}
    ]
};

const usersReducer = (state = initialState) => {
    return state;
};

export default usersReducer;